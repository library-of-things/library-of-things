import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#545E56',
    },
    secondary: {
      main: '#B79492',
    },
  },
});

export default theme;
